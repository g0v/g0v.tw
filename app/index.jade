doctype html
head
  meta(charset="utf8")
  script(type="text/javascript").
    (function(){
      var lang = window.navigator.language;
      if(lang === 'zh-TW')
        location.href = '/zh-TW';
      else
        location.href = '/en-US';
    }())
body
